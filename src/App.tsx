import { FC } from 'react';

interface AppProps {
  // Define your props here
  // Example: title?: string;
}

const App: FC<AppProps> = ({  }) => {
  return (
    <div>
      
    </div>
  );
};

export default App;